// Season-wide feature switches.
//
// PLAYOFFS_MODE swaps the regular splash screen for the playoff bracket splash.
// Set it back to false in the off-season — the regular SplashScreen is untouched
// and comes straight back.
export const PLAYOFFS_MODE = true;
